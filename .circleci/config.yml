# PHP CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/php:7.1-browsers

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mysql:9.4

    working_directory: ~/repo

    steps:
      - checkout

      - run: gcc -v
      - run: phpize && ./configure && make && sudo make install
      - run: composer install
      - run: ./tools/test.sh

      # - restore_cache:
      #     keys:
      #       - v1-phpbrew-{{ checksum "phpbrew"}}
      #       - v1-phpbrew
      # - run:
      #     name: Download phpbrew
      #     command: curl -L -O https://github.com/phpbrew/phpbrew/raw/master/phpbrew && chmod +x phpbrew

      # - save_cache:
      #     paths:
      #       - ./phpbrew
      #     key: v1-phpbrew-{{ checksum "phpbrew"}}

      # - run:
      #     name: phpbrew init
      #     command: ./phpbrew init && source ~/.phpbrew/bashrc

      # - restore_cache:
      #     keys:
      #       - v1-phpbrew-php-{{ checksum "~/.phpbrew/php-releases.json"}}
      #       - v1-phpbrew-php-

      # - run:
      #     name: install php7
      #     command: ./phpbrew install 7.1 +default

      # - save_cache:
      #     paths:
      #       - ~/.phpbrew
      #     key: v1-phpbrew-php-{{ checksum "~/.phpbrew/php-releases.json"}}

      # # Download and cache dependencies
      # - restore_cache:
      #     keys:
      #     - v1-dependencies-{{ checksum "composer.json" }}
      #     # fallback to using the latest cache if no exact match is found
      #     - v1-dependencies-

      # - run: composer install -n --prefer-dist

      # - save_cache:
      #     paths:
      #       - ./vendor
      #     key: v1-dependencies-{{ checksum "composer.json" }}

      # # run tests!
      # - run:
      #     name: xx
      #     command: ls
